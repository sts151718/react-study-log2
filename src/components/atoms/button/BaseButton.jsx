import styled from 'styled-components';

export const BaseButton = styled.button`
  border: none;
  border-radius: 6px;
  padding: 6px 12px;

  &:hover {
    cursor: pointer;
    opacity: 0.75;
  }
`;
